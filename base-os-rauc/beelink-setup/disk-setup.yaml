grub_setup:
  install: True
  partition: 1
  config: /boot/grub/grub.cfg
partition_setup:
- partition: 2
  replace_patterns:
  - /etc/fstab
  install_to:
  - "/boot/grub/grub.cfg": "/test/grub.cfg"
  install:
  - ""
